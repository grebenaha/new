extends layout

block content
    h3 Add to Daily messages

    form(method='POST' action='')
        div.form-group
            label(for='recipient') Recipient:
            input#recipient.form-control.col-5(type='text' placeholder='+972xxxxxxxxx' name='recipient' required='true' value='' )
        button.btn.btn-primary(type='submit') Submit
    if errors
        ul
            for error in errors
                li!= error.msg